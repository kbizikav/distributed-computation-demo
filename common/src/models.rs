// A problem to calculate x^2 for the given x
#[derive(Clone, Debug)]
pub struct Problem {
    pub x: u64,
}

// A solution to the problem
#[derive(Clone, Debug)]
pub struct Solution {
    pub x_squared: u64,
}
